"""CargoDash test suite.

Written with the stdlib ``unittest`` framework so it runs with zero extra
dependencies (``python -m unittest discover``). The same TestCase classes
are also collected as-is by ``pytest`` once CI is in place.
"""
